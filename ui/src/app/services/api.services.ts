// import { parseBoolean } from '../../../../api/src/common/config';
// import { resolve, sep } from 'path';
// import * as dotenv from '.env';

// dotenv.config({ path: resolve(__dirname, `..${sep}..${sep}..${sep}.env`) });

// if (!parseBoolean(process.env['API_DISABLE_HTTPS'])) {
//   const API_ENDPOINT = `https://${process.env.API_HOST}:${process.env.API_PORT_HTTPS}`;
// } else {
//   const API_ENDPOINT = `https://${process.env.API_HOST}:${process.env.API_PORT_HTTP}`;
// }
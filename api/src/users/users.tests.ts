/**
import { describe, expect, test } from '@jest/globals';
import UsersController from './users.controller';

const testUserId = '';
const testUsername = '';

describe('API Endpoint -> /users/id/*', () => {
  test(`GET /users/id/${testUserId}`, () => {

  });
});

describe('API Endpoint -> /users/username/*', () => {
  test(`GET /users/username/${testUsername}`, () => {

  });
});
describe('API Endpoint -> /register');
**/
